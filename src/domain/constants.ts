/** Stroke penalty applied when the ball lands in a water hazard (rule: drop + 1 penalty). */
export const WATER_PENALTY_STROKES = 2;

/** Number of putts assigned when the ball lands on the green. */
export const DEFAULT_PUTT_COUNT = 2;

/** SVG coordinate space width in pixels (matches viewBox). */
export const SVG_VIEWPORT_WIDTH = 400;

/** SVG coordinate space height in pixels (matches viewBox). */
export const SVG_VIEWPORT_HEIGHT = 600;

/** Scale factor for the tree canopy highlight offset (relative to tree radius). */
export const TREE_CANOPY_HIGHLIGHT_SCALE = 0.2;

/** Y-axis offset for the water surface ripple highlight (upward, in SVG units). */
export const WATER_RIPPLE_Y_OFFSET = 5;
